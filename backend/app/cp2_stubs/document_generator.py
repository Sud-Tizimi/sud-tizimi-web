"""CP2 — DOCX / PDF generation from a session transcript. Stub."""


class DocumentGenerator:
    """Renders a session into a printable, formatted court document."""

    async def render(self, session_id: str) -> bytes:
        raise NotImplementedError(
            "CP2: DocumentGenerator.render not implemented yet"
        )
