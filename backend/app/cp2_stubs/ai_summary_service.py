"""CP2 — AI summary of a session transcript. Stub."""


class AISummaryService:
    """Generates structured summaries (participants, facts, key events, outcome)."""

    async def summarize(self, transcript: list[dict]) -> dict:
        raise NotImplementedError(
            "CP2: AISummaryService.summarize not implemented yet"
        )
