fastapi==0.115.4
uvicorn[standard]==0.32.0
websockets==13.1
pydantic==2.9.2
pydantic-settings==2.6.1
httpx==0.28.1
python-dotenv==1.0.1
python-multipart==0.0.12
email-validator==2.2.0
openai==1.95.1
google-genai==1.20.0

# Database (Phase A)
sqlalchemy[asyncio]==2.0.36
aiomysql==0.2.0
cryptography==43.0.1
alembic==1.13.3

# Auth (Phase A)
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
pyjwt==2.9.0

# File IO (Phase B — listed now so the import surface is ready)
aiofiles==24.1.0

# SudAI-Law-UZ — legal document analysis (Phase 27: SudAI integration)
pypdf==5.1.0
python-docx==1.1.2

# OCR (Phase D — UDIP engine ported: PaddleOCR / Tesseract / Gemini / Stub)
Pillow>=10.2.0
opencv-python-headless>=4.9.0
numpy>=1.26
pytesseract>=0.3.10
PyMuPDF>=1.23.0
openpyxl>=3.1.2
python-pptx>=0.6.23
striprtf>=0.0.26
# PaddleOCR — heavy, install separately if needed:
#   pip install paddlepaddle paddleocr
# Gemini OCR uses the existing google-genai client (no new dep).
